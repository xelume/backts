# Changesets

运行 `pnpm changeset` 记录版本意图，选择受影响的公共包和语义版本级别。
通过 `pnpm changeset status` 和 `pnpm changeset:check` 检查计划。
不要手改版本或 CHANGELOG；由 Release PR 执行 `pnpm version-packages`。

完整流程见 [版本与发布](../docs/releasing.md)。
