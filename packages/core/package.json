{
  "name": "@backts/core",
  "version": "0.1.0",
  "type": "module",
  "description": "BackTS HTTP framework for building native backend services with TypeScript.",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "analyze": "backts analyze --tests",
    "test:build": "backts build --tests"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@backts/cli": "workspace:*"
  }
}
