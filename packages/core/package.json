{
  "name": "@backts/core",
  "version": "0.0.3",
  "type": "module",
  "description": "Composable HTTP primitives for native TypeScript backend services.",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "analyze": "backts analyze --tests",
    "test:build": "backts build --tests"
  },
  "publishConfig": {
    "registry": "https://registry.qlqs.work/"
  },
  "devDependencies": {
    "@backts/cli": "workspace:*"
  }
}
